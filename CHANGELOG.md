# Changelog

## [v1.0.4] - 2025-07-3
### Fixed
- Fixed location level listing facility

## [v1.0.3] - 2025-06-13
### Added
- Medication history feature the focus group clients
- Updated `versionCode` to 5 and `versionName` to 1.0.3 for the `moh` product flavor in `build.gradle`.
- Set new `opensrp_url`, `opensrp_url_production`, and `guidebooks_url` for the `moh` flavor.
- Defined `LOCATION_HIERACHY`, `ALLOWED_LOCATION_LEVELS`, and `ALLOWED_LOCATION_LEVELS_DEBUG` arrays for location configuration.
- Set `DEFAULT_LOCATION` and `DEFAULT_LOCATION_DEBUG` for the `moh` flavor.
- Updated `DATABASE_VERSION` to 27.

### Fixed
- App mixing the list of villages and health facilities in the `moh` flavor.
- Minor configuration and environment variable updates for the `moh` flavor.

### Changed
- Improved build configuration for the `moh` product flavor.
- Updated the list of medicines and commodities in the `moh` flavor.

---

For a full list of changes, see the commit history.
